# 豆沙工作日志 - Plan-001 轮次 3/5

## 任务概述
- 任务：前端 API 路径修正
- 目标：统一前端 API 基础路径为 `/api`

## 完成内容
1. 修改 `F:\openclaw\code\frontend\src\utils\request.ts`
   - 添加 baseURL 配置：`baseURL: '/api'`
   - 使所有 API 请求自动加上 `/api` 前缀

## 修改详情
```typescript
import axios from 'axios'

const request = axios.create({
  baseURL: '/api',
  timeout: 5000
})

export default request
```

## 验证状态
- 代码修改完成
- TypeScript 检查因环境问题未能执行
- 文件已保存至目标路径

## 任务状态
已完成文件修改，等待后续验证