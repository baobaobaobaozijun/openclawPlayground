<!-- Last Modified: 2026-03-23 -->

# 🍖 酱肉执行清单（v2 计划）

**角色:** 后端工程师  
**负责人:** 酱肉 (Jiangrou)  
**关联计划:** `doc/guides/博客系统-开发计划-v2.md`  
**关联技术方案:** `doc/01-core/tech/博客系统 - 技术方案.md`

---

## 阶段 1：基础设施（03-23 ~ 03-25）

### T1-05: 数据库 DDL 脚本
**截止:** 03-25 18:00  
**输出文件:** `F:\openclaw\code\backend\scripts\schema.sql`

**具体内容（从技术方案第 5 章翻译）：**

```sql
-- 必须包含以下 5 张表：
-- 1. users (用户表) — 字段见技术方案 5.2
-- 2. articles (文章表) — 含 user_id 外键
-- 3. categories (分类表) — 树形结构
-- 4. tags (标签表)
-- 5. article_tags (文章-标签关联表，多对多)

-- 必须包含的索引：
-- users: idx_phone (UNIQUE)
-- articles: idx_user_id, idx_status, idx_created_at, idx_category_id
-- article_tags: idx_article_id, idx_tag_id
```

**验证命令:**
```powershell
Test-Path F:\openclaw\code\backend\scripts\schema.sql
# 内容验证：应包含 CREATE TABLE users, articles, categories, tags, article_tags
```

### T1-06: 后端项目可运行
**截止:** 03-25 18:00  
**前置:** 酸菜完成 T1-01~T1-04

**逐项检查清单：**

| 序号 | 检查项 | 文件 | 验证命令 |
|------|--------|------|----------|
| 1 | pom.xml 依赖完整 | `code/backend/pom.xml` | `mvn dependency:resolve` |
| 2 | application.yml 配置正确 | `code/backend/src/main/resources/application.yml` | 检查 DB/Redis 连接串 |
| 3 | Entity 与 DDL 一致 | `entity/*.java` vs `scripts/schema.sql` | 人工比对 |
| 4 | 编译通过 | - | `mvn compile` |
| 5 | 启动成功 | - | `mvn spring-boot:run`（无报错退出） |

---

## 阶段 2：核心 API（03-26 ~ 03-29）

### T2-01: 用户认证 API
**截止:** 03-27 18:00  
**输出文件及验证：**

| 文件 | 内容 | 验证 |
|------|------|------|
| `controller/AuthController.java` | 已有框架，补充完整逻辑 | POST /api/auth/login 返回 JWT |
| `service/impl/UserServiceImpl.java` | 注册/登录业务逻辑 | 手机号格式校验+自动注册 |
| `security/JwtAuthenticationFilter.java` | JWT 过滤器 | Token 验证通过 |
| `util/JwtUtil.java` | Token 生成/解析 | 2 小时有效期 |

**Postman 测试用例（必须通过）：**
1. `POST /api/auth/login` — 新手机号自动注册，返回 token
2. `POST /api/auth/login` — 已有手机号登录，返回 token
3. 携带 token 访问受保护接口 — 200
4. 无 token 访问受保护接口 — 401
5. 过期 token — 401

### T2-02: 文章 CRUD API
**截止:** 03-28 18:00  
**输出文件及验证：**

| 文件 | 内容 | 验证 |
|------|------|------|
| `controller/ArticleController.java` | 已有框架，补充完整 | 6 个接口全部可用 |
| `service/impl/ArticleServiceImpl.java` | CRUD + 分页 + 搜索 | 分页参数正确 |
| `dto/ArticleCreateDTO.java` | 已有 | 字段验证注解 |

**接口清单（参考技术方案 4.1）：**
1. `GET /api/articles` — 文章列表（分页+状态筛选）
2. `GET /api/articles/{id}` — 文章详情
3. `POST /api/articles` — 创建文章（需登录）
4. `PUT /api/articles/{id}` — 更新文章（需作者权限）
5. `DELETE /api/articles/{id}` — 删除文章（需作者/管理员）
6. Markdown → HTML 渲染

### T2-03: 分类/标签 API
**截止:** 03-29 12:00  
**缺失文件（需新建）：**

| 文件 | 说明 |
|------|------|
| `controller/CategoryController.java` | **新建** |
| `controller/TagController.java` | **新建** |
| `service/CategoryService.java` | **新建** |
| `service/TagService.java` | **新建** |
| `service/impl/CategoryServiceImpl.java` | **新建** |
| `service/impl/TagServiceImpl.java` | **新建** |
| `dto/CategoryDTO.java` | **新建** |
| `dto/TagDTO.java` | **新建** |

### T2-04: Swagger 文档
**截止:** 03-29 18:00  
**输出：** Swagger UI 可通过 `http://localhost:8080/swagger-ui.html` 访问

**需要：**
1. pom.xml 添加 springdoc-openapi 依赖
2. 各 Controller 添加 @Operation 注解
3. 验证所有接口在 Swagger 中显示正确

---

## 工作日志要求

每完成一个子任务，必须写工作日志到 `workinglog/jiangrou/`：
- 文件名：`YYYYMMDD-hhmmss-jiangrou-{任务ID}.md`
- 内容：修改了哪些文件、验证结果、下一步计划

---

*灌汤 (PM) | 2026-03-23*
