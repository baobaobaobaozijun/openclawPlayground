# OpenClaw Agent 团队 - Python 依赖包
# 使用方法：pip install -r requirements.txt

# ======================================
# 核心依赖（必需）
# ======================================

# HTTP 请求库（用于 API 调用）
requests==2.31.0

# 定时任务调度（用于日志提交、监控等）
APScheduler==3.10.4

# 环境变量管理
python-dotenv==1.0.0

# 系统资源监控
psutil==5.9.6

# ======================================
# 后端开发依赖（酱肉 - 可选）
# ======================================

# Web 框架（二选一）
Flask==3.0.0
# Django==4.2.7  # 如果选择 Django，取消注释

# JWT 认证
Flask-JWT-Extended==4.6.0
PyJWT==2.8.0

# ORM
SQLAlchemy==2.0.23
# Flask-SQLAlchemy==3.1.1  # 如果使用 Flask

# 密码加密
Werkzeug==3.0.1

# 数据库迁移（可选）
alembic==1.13.0

# ======================================
# 前端开发依赖（豆沙 - 可选）
# ======================================

# 注意：前端主要使用 Node.js/npm，Python 依赖较少
# 如果需要，可以添加一些构建工具

# ======================================
# 运维测试依赖（酸菜 - 可选）
# ======================================

# 测试框架
pytest==7.4.3
pytest-cov==4.1.0

# API 测试
responses==0.24.1

# 性能测试
locust==2.20.0

# 代码质量检查（可选）
flake8==7.0.0
black==23.12.1

# ======================================
# 博客集成依赖（阶段 2 - 可选）
# ======================================

# 如果需要在本地运行博客后端
# Flask==3.0.0  # 已包含

# Markdown 渲染
markdown==3.5.1

# ======================================
# 开发工具（可选）
# ======================================

# 调试工具
ipython==8.18.1
pdbpp==0.10.3

# 文档生成
mkdocs==1.5.3
mkdocs-material==9.5.3

# ======================================
# 说明
# ======================================
# 
# 最小化安装（仅核心功能）:
#   pip install requests APScheduler python-dotenv psutil
#
# 完整安装（所有功能）:
#   pip install -r requirements.txt
#
# 按需安装（例如只安装后端依赖）:
#   pip install flask flask-jwt-extended sqlalchemy
#
# ======================================
